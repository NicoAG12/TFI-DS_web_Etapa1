apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-config
  namespace: tfi-prod
data:
  AI_ASSISTANT_PORT: "4401"
  VECTOR_DB_URL: "http://vector-db:8000"
  LLM_PROVIDER: "openai"
  EMBEDDING_PROVIDER: "openai"
  LLM_TIMEOUT_MS: "5000"
  NOTIFICATION_LLM_TIMEOUT_MS: "3000"

