apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-ai-worker
  namespace: tfi-prod
  labels:
    app: notification-ai-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-ai-worker
  template:
    metadata:
      labels:
        app: notification-ai-worker
    spec:
      containers:
        - name: notification-ai-worker
          image: ghcr.io/mi-organizacion/notification-ai-worker:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: ai-config
            - secretRef:
                name: ai-secrets
            - configMapRef:
                name: app-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

